<skill id="safe-store-rag">
    <name>safe_store: Complete RAG Pipeline</name>
    <description>End-to-end retrieval-augmented generation integration patterns</description>
    <category>python/safe_store/rag</category>
    <language>markdown</language>
    <timestamp>1740265200000</timestamp>
    <content>
        <![CDATA[
## RAG Pipeline Patterns

### 1. Basic RAG Loop
```python
from safe_store import SafeStore

with store:
    results = store.query(question, top_k=3)

context = "\n\n".join([f"[Source: {r['file_path']}]\n{r['chunk_text']}" for r in results])
prompt = f"Context:\n{context}\n\nQuestion: {question}\nAnswer:"

# Generate with LLM...
```

### 2. Cited RAG
```python
numbered_context = []
for i, r in enumerate(results, 1):
    numbered_context.append(f"[{i}] From {r['file_path']}:\n{r['chunk_text']}")

prompt = f"Use numbered sources to answer. Cite as [1], [2].\n\n{'\n\n'.join(numbered_context)}\n\nQuestion: {question}"
```
]]>
    </content>
</skill>